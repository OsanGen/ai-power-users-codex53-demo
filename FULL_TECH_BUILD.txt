# FULL_TECH_BUILD

## 1) Snapshot header
- UpdatedAt: 2026-03-01T01:59:15Z
- RepoName: WONDERLAND
- Branch, HEAD commit hash: main, 33ca076
- Dirty state: yes (untracked: node_modules/, output/, test-results/)
- Primary languages + package managers detected: JavaScript, HTML, CSS, JSON | npm

## 2) What this repo is
WONDERLAND is a browser-first vanilla web game (canvas-based), centered on a floor-progression shooter loop with HUD overlays, tutorial/lesson flow, upgrade progression, and share/replay-oriented UI controls. It ships as an interactive web app with static assets and QA hooks.

## 3) System map
- Components: `index.html`, `src/main.js`, `src/core.js`, `src/systems.js`, `src/render.js`, `src/audio.js`, `src/share.js`, `src/ui-tokens.js`, `src/content.js`, `src/upgrades.js`, `src/ui-tokens.js`.
- Boundaries: `index.html` loads modules; `core.js` wires shared globals; `main.js` owns loop/bootstrap/wiring; `systems.js` owns simulation/input/state transitions; `render.js` owns canvas rendering and FX pipeline; `audio.js` owns music/SFX + persistence; `share.js` owns modal/copy/share interactions.
- Key data flow: DOM/input/URL/state seed → simulation tick/update (systems) → draw pipeline (render) → overlay/audio/mute/share outputs.
- If DEV MODE is active, `final_build/BUILD_MAP.txt` is the canonical system map.

## 4) Entrypoints and run commands
- Local run: `python3 -m http.server 4173` then open `http://127.0.0.1:4173/index.html`.
- Build: none (static app).
- QA: `npm run qa:bootstrap`, `npm run qa:bootstrap:both`, `npm run qa:deps-contract`, `npx playwright test wonderland-qa.spec.js`, `node scripts/check-ui-bootstrap-oneshot.js`.
- Lint/format/typecheck: none centralized in package scripts; ad hoc checks are mostly Playwright + dependency contract checks.
- Environment notes: `QA_TARGET_URL`, `QA_STAGED_URL`, `QA_DEPLOYED_URL` for bootstrap routes.

## 5) Dependencies and toolchain summary
- Package manager: npm
  - Manifest(s): `package.json`
  - Lockfile(s): `package-lock.json`
  - Direct dependencies:
    - `@playwright/test` (dev) `^1.58.2`
  - Command to print dependency tree: `npm ls --depth=0`
- Toolchain: Node + Playwright + script-driven QA.
- Known risks: dependency manifest/lockfile mismatch, local script additions not reflected in lockfile checks, and static cache-bust rollout friction across deployed pages.

## 6) Current implementation status
- What is implemented: unified runtime DOM contracts, deterministic bootstrap checks, dependency contract validation, render telemetry and adaptive render performance governor scaffold, render-mode debug exports, and extended `render_game_to_text` diagnostics. Muzzle flash projectile and socket alignment now uses per-direction radius-scaled gun sockets.
- What is in progress: deterministic performance baseline + benchmark assertions for `debug.renderPerf` stability and low/high-tier switching behavior.
- What is not started: formal rollout guard for `renderPerf` mode visibility/telemetry in long-running QA sequence and optional off-by-default perf script wiring in normal suite. A visual verification pass for the up-socket flash-color fix is the next immediate acceptance step.
- If DEV MODE is active, `final_build/CURRENT_GAPS.txt` holds active gaps and `final_build/COMPONENT__*.txt` holds component specs.

## 7) Recent change log (rolling)
- 2026-03-01 | Delayed directional burst dual unlock from 2s to 4s of continuous shooting by updating `REAR_SHOT_TRIGGER_SECONDS` in gameplay constants; omni unlock threshold remains unchanged. | src/core.js
- 2026-03-01 | Tuned up socket calibration for upward shots to `{x:-2, y:-24}` at base radius and narrowed muzzle flash core-to-white blend so flash tint follows projectile colors (`yellow/blue/mint/pink`) while keeping gameplay behavior unchanged. | src/systems.js, src/render.js
- 2026-03-01 | Replaced coarse muzzle anchor math with direction-specific radius-scaled socket offsets and added `AIPU.systems.getMuzzleAnchorForDirection()` to keep bullet/muzzle origin aligned per cardinal direction; added debug-only render socket marker overlay for all sockets with active emphasis. | src/systems.js, src/render.js
- 2026-02-28 | Increased muzzle flash reach and shifted muzzle flash rendering to projectile-color-dominant bloom/streak composition for clearer shot-to-bullet continuity | src/render.js
- 2026-02-28 | Commit 96b64ff: Added adaptive render governor state machine with query/localStorage modes, frame pressure hysteresis, quality-tier routing, and `debug.renderPerf` diagnostics | src/render.js
- 2026-02-28 | Added QA bootstrap dependency contract enforcement and script updates for staged/deployed smoke checks | scripts/check-dependency-contract.js, scripts/check-ui-bootstrap.sh, scripts/check-ui-bootstrap-oneshot.js, package.json
- 2026-02-28 | Added UI element contract cleanup and shared selector hygiene across runtime handlers | src/ui-tokens.js, index.html, src/core.js, src/main.js, src/share.js, src/systems.js
- 2026-02-28 | Added audio muting migration/persistence hardening with independent music/SFX state and UI sync coverage hooks | src/audio.js, src/systems.js
- 2026-02-27 | Added floor-water wrapper rendering and debug state output (`debug.visualState`) in render snapshots | src/render.js

## 8) Refresh procedure
- `git rev-parse --short HEAD`
- `git status --short`
- `cat package.json`
- `cat package-lock.json`
- `rg -n "getElementById|querySelector|id=\"" index.html src/core.js src/main.js src/share.js src/systems.js`
- `cat final_build/CURRENT_GAPS.txt`
- `cat final_build/BUILD_MAP.txt`
- `node scripts/check-dependency-contract.js`
